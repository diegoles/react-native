import React from 'react'
import { View, Text } from "react-native";

export default function AddRestaurantForm(params) {

    const{toastRef, navigation} = params;
    return(
        <View>
            <Text>Add Restaurant Form</Text>
        </View>
    );
    
}